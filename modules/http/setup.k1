// Compiles the vendored llhttp release C (v9.4.1, generated once from the
// nodejs/llhttp repo) into libs/libllhttp.a and regenerates the llhttp
// bindings. Runs in the compile-time VM with cwd = the module dir; needs cc
// and k1bindgen on PATH.
use std/process

fn setup(ctx: k1/setup-ctx) {
  let _ = process/run-command("mkdir", ["-p", "libs", ".k1-out/llhttp"], :inherit).!

  for ["llhttp", "api", "http"] {
    let _ = process/run-command("cc", ["-O2", "-Ivendor", "-c", "vendor/${it}.c", "-o", ".k1-out/llhttp/${it}.o"], :inherit).!
  }
  let _ = process/run-command("/bin/sh", ["-c", "ar rcs libs/libllhttp.a .k1-out/llhttp/*.o"], :inherit).!

  let args: list[string] = [
    "-in-path", "vendor/llhttp.h",
    "-out-path", "llhttp.k1",
    "-lib-name", "llhttp",
    "+strip-prefix", "llhttp_",
    "+strip-prefix", "HTTP_",
    "+strip-suffix", "_t",
  ]
  // The host SDK, for system headers
  if k1/os-macos {
    let sdk = process/run("/bin/sh", ["-c", "printf %s $(xcrun --sdk macosx --show-sdk-path)"]).!
    args.push("+clangarg")
    args.push("-isysroot")
    args.push("+clangarg")
    args.push(sdk)
  }
  let _ = process/run-command("k1bindgen", args.as-span(), :inherit).!
}
