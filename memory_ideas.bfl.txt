// heaps have a 'strings' section because strings are hard/special/important?

fn main(): int {
  rt.createPool("people");

  context val pool = rt.getPool("people");

  for [1,2,3,4,5] {
    val person = new({ name: "George", age: i });
    person
  }
}
