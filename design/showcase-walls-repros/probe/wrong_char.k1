fn main(): i32 {
  let greeting: "hello world" = #static {
    let bytes = list/empty[u8]()
    for c in "hello" { bytes.push(c.u8()) }
    bytes.push(' '.u8())
    for c in "world" { bytes.push(c.u8()) }
    string/wrap-bytes(bytes.as-span())
  }
  println(greeting.from-static())
  0
}
