ns stuff {
  type alias intalias = int
}

use stuff/intalias

type Bar = { foo: Foo, bar: *Bar, i: intalias }
type Foo = { x: t }

type alias t = opaque[4, 4]

fn main(argc: int, argv: ptr): i32 {

  0
}
