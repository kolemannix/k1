fn main(argc: int, argv: ptr): i32 {
  let clang-args = list/empty[string];
  // BUG: captured context parameter results in 'Missing variable' in ir
  //let context alloc: mem/alloc-mode = :arena;
  //let clang-args-c-strs: list[ptr] = clang-args.map(fn s. s.toCString().ptr);

  let clang-args-c-strs: list[ptr] = clang-args.map(fn s. s.toCString(context :arena)().ptr);

  0
}
