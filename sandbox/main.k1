fn main(): i32 {
  let a = core/arena/init-virtual(0);
  a.mark();
  for IntRange/upTo(100) { a.alloc[u64](); };

  println("a is at {a.usedBytes()}");
  a.reset(false);
  println("a is at {a.usedBytes()}");

  0
}
