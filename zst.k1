// nocommit convert and commit test case
let g: {} = {};
let* g2: *{} = {};

fn testGlobal(): empty {
  assertEquals(0, types/sizeOf[typeOf(g)]);
  g2;
  assertEquals(g, g2.*);
}

fn testLocal(_a: empty, _b: empty, _c: empty, _d: empty): empty {
  let l = {}; 
  assertEquals(types/sizeOf[typeOf(l)], 0);
  l
}

fn main(): i32  {

  g; // empty
  g2; // null ptr
  g2.*; // no-op yielding Value::Empty

  testLocal({}, {}, {}, {});
  0
}
