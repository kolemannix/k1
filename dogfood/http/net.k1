ns net; // generated by k1-bindgen

use core/c-int
type alias in_addr_t = u32

type alias in_port_t = __uint16_t

type alias sa_family_t = __uint8_t

type sockaddr = {
  sa_len: __uint8_t,
  sa_family: sa_family_t,
  sa_data: array[i8, 14],
}

type in_addr = {
  s_addr: in_addr_t,
}

type sockaddr_in = {
  sin_len: __uint8_t,
  sin_family: sa_family_t,
  sin_port: in_port_t,
  sin_addr: in_addr,
  sin_zero: array[i8, 8],
}

