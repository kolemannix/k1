ns kqueue; // generated by k1-bindgen

use core/c-int
// BINDGEN ERROR: __mbstate_t: unions aren't supported yet (size 128 align 8)
deftype alias __darwin_socklen_t = __uint32_t
deftype kevent = {
  ident: ptr,
  filter: i16,
  flags: u16,
  fflags: u32,
  data: ptr,
  udata: ptr,
}
extern("kqueue", "kevent") fn kevent(
  p0: c-int,
  p1: *mut kevent,
  p2: c-int,
  p3: *mut kevent,
  p4: c-int,
  p5: *mut timespec,
): c-int

