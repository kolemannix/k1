val max_bound: Int := 42;

type Point = struct {
  x: Int,
  y: Int,
}

fn main(args: List[String]): Result[Int] {
  println("Hello, World!");
  val x: Int := 0;
  mut y: Int := 1;
  y      := y + max_bound;
  return 0;
}
