fn identity[T](t: T): T { return t; }

fn main(): int {
  printInt(identity[int](4));
  // printInt(identity[bool](true));
  // printInt(identity[{ value: int }]({ value: 42 }).value);
  return 0;
}
// 4