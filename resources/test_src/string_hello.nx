fn main(): int {
  val x: string = "abc ABC";
  mut i: int = 0;
  print("x.length is:");
  printInt(x.length());
  while i < (x.length() - 1) {
    x[i];
    i = i + 1;
  };
  print(x);
  return 0;
}
