val z: int = 1337;

fn add_stuff(x: int, y: int): int {
  val b: int = x + 1;
  val foo: int = b + z;
  return foo + y;
}

fn main(x: int): int {
  val result: int = add_stuff(y = 2, x = 1);
  printInt(result);
  return 1341;
}
// <expected output>
// 1341
