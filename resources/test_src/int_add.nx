val z: Int = 1337;

fn add_stuff(x: Int, y: Int): Int {
  val b: Int = x + 1;
  val foo: Int = b + z;
  return foo + y;
}

fn main(x: Int): Int {
  val result: Int = add_stuff(y = 2, x = 1);
  println(result);
  return 1341;
}
// <expected output>
// 1341
