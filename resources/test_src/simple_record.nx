type Point = {
  x: int,
  y: int,
  z: int,
  isGood: bool
}

fn main(): int {
  val zero: Point = {
    x: 0, y: 298, z: 42, isGood: true
  };
  printInt(zero.z);
  printInt(zero.isGood);
  printInt(zero.y);
  return zero.y;
}
// 298
