type Point = {
  x: Int,
  y: Int,
  z: Int,
  isGood: Bool
}

fn main(): Int {
  val zero: Point = {
    x: 0, y: 298, z: 42, isGood: true
  };
  printInt(zero.z);
  printInt(zero.isGood);
  printInt(zero.y);
  return 0;
}
