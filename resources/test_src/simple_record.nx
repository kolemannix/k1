type Point = {
  x: Int,
  y: Int,
  z: Int,
  isGood: Bool
}

fn main(): Int {
  val zero: Point = {
    x: 0, y: 0, z: 42, isGood: true
  };
  println(zero.z);
  println(zero.isGood);
  return 0;
}
