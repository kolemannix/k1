type Point = {
  x: Int,
  y: Int,
  z: Int,
  isGood: Bool
}

fn main(): Int {
  val zero: Point = {
    x: 0, y: 298, z: 42, isGood: true
  };
  println(zero.z);
  println(zero.isGood);
  println(zero.y);
  return 0;
}
