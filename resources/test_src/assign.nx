fn foo(): Int {
  mut result: Int = 0;
  if true {
    result = 11;
  } else {
    result = 12;
  };
  return result;
}

fn main(): Int {
  printInt(foo());
  mut count: Int = 0;
  count = 1;
  printInt(count);
  count = 2;
  printInt(count);
  return 0;
}
