type Point = {
  x: int,
  y: int,
  z: int,
  isGood: bool
}

fn main(): int {
  val zero: Point = {
    x: 0, y: 298, z: 42, isGood: true
  };
  printInt(zero.z);
  printInt(if zero.isGood 1 else 0);
  printInt(zero.y);
  zero.y
}
// 42, 1, 298
