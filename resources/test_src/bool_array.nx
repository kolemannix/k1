fn boolToInt(b: bool): int {
  return if b 1 else 0;
}
fn main(): int {
  val boolArray: Array<bool> = [false,true];
  printInt(boolToInt(boolArray[0]));
  printInt(boolToInt(boolArray[1]));
  return 0;
}
// 0, 1
