type Point = {
  x: Int,
  y: Int,
  z: Int,
  isGood: Bool
}

// fn increment_z(p: Ref[Point]): void fails Int {
//   p.z = p.z + 1;
// }

fn main(): Int {
  val zero: Point = {
    x: 0, y: 0, z: 42, isGood: true
  };
  println(zero.z);
  return 0;
}
