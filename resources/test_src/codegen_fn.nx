val z: Int = 1337;

fn add(x: Int, y: Int): Int {
  val b: Int = x + 1;
  val foo: Int = b + z;
  return foo + y;
}

fn main(x: Int): Int {
  val result: Int = add(1,2);
  println(result);
  return 0;
}
