// heaps have a 'strings' section because strings are hard/special/important?

fn main(): i32 {
  rt.createPool("people");

  context let pool = rt.getPool("people");

  for [1,2,3,4,5] {
    let person = new({ name: "George", age: i });
    person
  }
}
