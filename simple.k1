//let x: int = #static {
//  3 * 4
//};

fn myassert(context locn: core/compiler/SourceLocation)(value: bool): unit {
  if not value { crash("ASSERT FAILED") };
}

fn foo(context env: string)(_unused: u8, usedTwice: f32): int {
  let f = usedTwice;
  let f2 = usedTwice;
  let x = 1;
  println("my env is {env}!");
  x + 1
}

fn main(): i32 {
  \(x: int, y: int) x + x;

  let context env: string = "stuff_in_env";
  foo(3, 3.14);
  0
}

