fn unique(arr: Array<char>): bool {
  return true;
}

fn main(): int {
  val input = "adfasdfasdfasdffd";
  mut i = 0;
  while (i < input.length()) {
    val sub = [input[i], input[i + 1], input[i + 2], input[i + 3]];
    if unique(sub) {
      printInt(i + 4);
    };
    i = i + 1;
  };
  return 0;
}