fn main(): int {
  mut maybe_x: string? = None;
  // maybe_x = "hello";
  if maybe_x |x| {
    // This gives assert failed instead of taking the else branch
    assert(x.length() == 5);
  } else {
    print("none");
  };
  0
}
