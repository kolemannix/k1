fn len<T>(x: Array<T>, y: Array<T>): int { x.length() + y.length() }

fn main(): int {
  val xs = [1,2,3];
  val ys = [true];
  len(xs, ys);
  Array::length(xs);
  xs.length();
  identity(xs).length();
//  xs.length();
  0
}
