type alias PointList = Array<Point>

type alias Point = { x: Barzle, y: int }
type Barzle = { x: Foobear }
type Foobear = { y: Lulz }
type Lulz = { z: int }

// Fails when Point is defined inside S
namespace S {
  type alias Point = { x: Barzle, y: int }
}

type Stuff = { ps: PointList, p: Point, b: Barzle, f: Foobear, l: Lulz }

fn main(): int {
    val s: Array<Stuff> = [];
    0
}
