fn main(): int {
  let x: string = "abc";
  let conc = x.concat(x);
  // printInt(conc.length());
  // println(conc);
  // println("abcabc");
  assert(conc == "abcabc");
  assert(conc.len == 6);
  0
}
