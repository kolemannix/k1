deftype Maybe = either A , B(char) , C(string)

fn main(): int {
  let a: Maybe = .A;
  let b: Maybe = .B('x');
  let c: Maybe = .C("hello, enums");
  if a is .A {
    ()
  } else assert(false);
  if b is .B(x) {
    assert(x == 'x');
  } else assert(false);
  if c is .C("hello, enums") {
    ()
  } else assert(false);
  0
}
