ns enum_fail;
ns inner;
type E = either { A(int) , B , C }

fn test() {
  let result = testCompile(:A(true): E);
  assertEquals(
    result,
    :some(":A payload type mismatch: Expected i64 but got bool")
  );
}
