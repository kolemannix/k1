ns allocator_test;

fn test() {
  let* a = core/Arena/default();

  a.push(true);
  a.push({ x: 10, y: 10 });
  let s = a.push("Hello, memory");
  assert(s.len() == 13);
  assertEquals(s.*, "Hello, memory");

  a.release();

}
