ns lexical_scope;

fn test() {
  let result = testCompile({
    { let _foo = 100 };
    assert(_foo == 100);
  });
  assert(
    result is .some(msg) 
    and msg.startsWith("Error in parameter 'value' in call to 'assert'\nVariable '_foo' is not defined")
  )
}
