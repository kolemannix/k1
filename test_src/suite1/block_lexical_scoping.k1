ns lexical_scope;

fn test(): unit {
  let result = testCompile({
    { let foo = 100 };
    assert(foo == 100);
  });
  assert(result is .Some(msg) and msg.startsWith("Variable 'foo' is not defined"))
}
