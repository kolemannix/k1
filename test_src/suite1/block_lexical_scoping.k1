ns lexical_scope;

fn test() {
  let result = testCompile({
    { let _foo = 100 };
    assert(_foo == 100);
  });
  assert(
    result is .some(msg) 
    and msg.startsWith("Invalid call to assert\nInvalid type for parameter 'value'\nVariable '_foo' is not defined")
  )
}
