ns lexical_scope;

fn test() {
  let result = testCompile({
    { let _foo = 100 };
    assert(_foo == 100);
  });
  assert(
    result is .Some(msg) 
    and msg.startsWith("Invalid call to assert\nInvalid type for parameter 'value': Variable '_foo' is not defined")
  )
}
