ns ability_fail;
ability num {
  fn num(self): i32
}

type Tool = either { True, False, Iono }

fn test() {
  let p: Tool = :True;
  let result = testCompile(p.num());
  assert-equals(result, :some(
`Call to num/num with self := Tool does not work
No matching implementations found`
  ))
}
