ns type_info;

use core/types/size
use core/types/stride
use core/types/align
use core/types/type-id

fn doIt(_foo: bool, _bar: u32): u32 {
  32
}

fn testSizeOf() {
  assert-equals(size[string], 16);
  assert-equals(stride[string], 16);
  assert-equals(align[string], 8);
  assert-equals(size[{ x: u64, c: u8 }], 9);
  assert-equals(stride[{ x: u64, c: u8 }], 16);
}

fn testFnTypeInfo() {
  assert-equals(type-id[doIt.return](), type-id[u32]());
  assert-equals(type-id[doIt._foo](), type-id[bool]());
  assert-equals(type-id[doIt._bar](), type-id[doIt.return]());
}

fn test() {
  testSizeOf();
  testFnTypeInfo();
}
