ability Num {
  fn num(self: Self): i32
}

deftype Tool = enum True , False , Iono

fn main(): int {
  val p: Tool = .True;
  assert(p.num() == 1);

  0
}
//errmsg: Method 'num' does not exist on type Tool
