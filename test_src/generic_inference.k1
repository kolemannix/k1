fn combinedLength[T](x: Array[T], y: Array[T]): u64 {
  x.len + y.len
}

fn identity[T](x: T): T {
  x
}

fn main(): int {
  val xs = [1,2,3];
  val ys = [4,5,6];

  identity(xs).len;

  combinedLength(xs, ys);
  0
}
