fn combinedLength[T](x: Array[T], y: Array[T]): u64 {
  x.length() + y.length()
}

fn identity[T](x: T): T {
  x
}

fn main(): int {
  val xs = [1,2,3];
  val ys = [4,5,6];

  xs.length();

  identity(xs).length();

  combinedLength(xs, ys);
  0
}
