type Point = { x: int, y: int? }

fn main(): int {
  val p: Point? = { x: 1, y: 2 };
  assert(p!.x == 1);
  assert(p!.y! == 2);

  val b = true: bool?;
  if b is Some(x) {
    assert(x);
  };
  assert(b.hasValue());
  val c: int? = None;
  assert(not c.hasValue());

  assert(typeId[Point.y.value]() == typeId[int]());

  0
}
