type Maybe = enum A, B(char), C(string)

fn main(): int {
  val foo: Maybe = .B('x');
  // nocommit: Test with side-effecting match targets
  if foo is .B(x) {
    print("y")asdfasdf;
    print(x.toString());
    //assert(x == 'x');
  } else {
    print("n");
    //assert(false);
  };
  0
}
